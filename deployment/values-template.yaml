# Environment Configuration Values
# This file gets populated by deployment scripts from the current Azure environment
# DO NOT commit this file - it's generated dynamically

# Azure Environment
azureSubscriptionId: ""
azureTenantId: ""
resourceGroupName: ""

# Container Registry
acrName: ""
acrLoginServer: ""
imageName: "respondr"
imageTag: "latest"

# Azure OpenAI
azureOpenAIEndpoint: ""
azureOpenAIDeployment: ""
azureOpenAIApiVersion: ""

# Storage Configuration
storageBackend: "redis"  # Options: redis, azure_table, file, memory
storageFallback: "memory"  # Options: memory, file, redis (different from primary)

# Azure Storage (when using azure_table backend)
azureStorageAccountName: ""
azureStorageAccountKey: ""
azureStorageTableName: "responderMessages"

# Redis Configuration (when using redis backend)
redisHost: "respondr-redis"
redisPort: "6379"
redisDb: "0"

# OAuth2 Configuration
oauth2ClientId: ""
oauth2TenantId: ""  # kept for backward compatibility if single-tenant is desired
multiTenantAuth: true
allowedEmailDomains:
	- scvsar.org

# admin users
allowedAdminUsers:
    - "user1@domain.org"
    - "user2@domain.org"	
oauth2RedirectUrl: ""

# Domain Configuration
domain: ""
hostname: ""

# Application Configuration
namespace: "respondr"
appName: "respondr"
replicas: 2
