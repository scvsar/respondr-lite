{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Smoke: list files to ensure project structure intact",
			"type": "shell",
			"command": "pwsh",
			"args": [
				"-NoLogo",
				"-NoProfile",
				"-Command",
				"Get-ChildItem -Recurse -Depth 2 | Select-Object FullName | Out-String | Write-Output"
			]
		}
	]
}