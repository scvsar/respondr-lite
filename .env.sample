# Common
# Allowed email domains (comma-separated)
ALLOWED_EMAIL_DOMAINS=example.com,example.org

# Comma-separated admin user emails
ALLOWED_ADMIN_USERS="bob@example.com,alice@example.com"

# Storage
# Keep connection strings and keys out of source control. Fill in in your
# deployment environment or local .env (do NOT commit secrets).
AZURE_STORAGE_CONNECTION_STRING=
STORAGE_TABLE_NAME=RespondrMessages
STORAGE_QUEUE_NAME=RespondrIncoming

# Security
WEBHOOK_API_KEY=
BACKEND_API_KEY=
# When `WEBHOOK_API_KEY` is empty (e.g. GroupMe can't send custom headers),
# set `ALLOWED_GROUPME_GROUP_IDS` to a comma-separated list of GroupMe group ids
# that are allowed to post. Example:
# ALLOWED_GROUPME_GROUP_IDS=102193274,109174633,97608845
# If left empty, the function will accept unauthenticated requests (not recommended).
ALLOWED_GROUPME_GROUP_IDS=

# LLM / Azure OpenAI
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=
# Use the API version your deployment supports. Example: 2024-12-01-preview
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# App behavior
TIMEZONE=America/Los_Angeles
DEBUG_FULL_LLM_LOG=false
RETENTION_DAYS=30

# LLM tuning / limits
# Maximum tokens allowed for completion by default
DEFAULT_MAX_COMPLETION_TOKENS=4096
# Minimum tokens to request when adjusting
MIN_COMPLETION_TOKENS=2048
# Absolute cap we will not exceed
MAX_COMPLETION_TOKENS_CAP=16384
# Retry behavior for LLM calls
LLM_MAX_RETRIES=3
# Factor to increase tokens when retrying
LLM_TOKEN_INCREASE_FACTOR=1.5

# Debug / local dev flags
DEBUG=false
# When true, allow bypassing auth locally (only for development)
ALLOW_LOCAL_AUTH_BYPASS=true
LOCAL_BYPASS_IS_ADMIN=true
# Allow clearing all stored messages via the UI/API (use with caution)
ALLOW_CLEAR_ALL=true
