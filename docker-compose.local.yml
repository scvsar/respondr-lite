services:
  respondr:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DEBUG=false
      - DISABLE_API_KEY_CHECK=true
      - ALLOW_LOCAL_AUTH_BYPASS=true
      - LOCAL_BYPASS_IS_ADMIN=true
      - ALLOW_CLEAR_ALL=true
      - ENABLE_LOCAL_AUTH=true
      - INACTIVITY_TIMEOUT_MINUTES=1
    env_file:
      - .env
    volumes:
      - ./backend:/app/backend:ro  # Mount backend source under /app/backend to avoid overwriting build artifacts
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/storage-info"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
 
  # Optional: Add nginx for reverse proxy testing
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - respondr
    profiles:
      - nginx  # Only start with --profile nginx
 

 