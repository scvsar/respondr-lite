{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "func",
      "label": "func: host start",
      "command": "host start",
      "problemMatcher": "$func-python-watch",
      "isBackground": true,
      "dependsOn": "deps (uv)"
    },
    {
      "label": "deps (uv)",
      "type": "shell",
      "command": "uv pip install -r ${workspaceFolder}/requirements.txt",
      "options": {
        "env": {
          "UV_PROJECT_ENVIRONMENT": ".venv"
        }
      },
      "problemMatcher": []
    }
  ]
}
